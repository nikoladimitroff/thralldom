module Thralldom {
    export class DynamicObject extends LoadableObject {
        public centerToMesh: THREE.Vector3;
        public isAirborne: boolean;

        public update(delta: number): void {

        }
    }
}